// Retired: model connectors live on Routers only. Kept as an empty module so stale
// dynamic imports fail closed rather than resurrect the legacy global chain UI.
export default function ModelsPage() {
  return null;
}
