// Persona contextSections and toolBundle values are persisted persona data.
// Configure them through PersonaStorage-backed API/tool mutations; do not define
// canonical bundle values in source or reconcile them during process boot.
