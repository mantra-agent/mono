# Council — retired

The Council deliberation subsystem has been removed.

Session-tree ownership lives in `server/sessions/AGENTS.md` and `server/sessions/tree.ts`.
Do not reintroduce council orchestration, advocate model pins, or a parallel deliberation runner.
